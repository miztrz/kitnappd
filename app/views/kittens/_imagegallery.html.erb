<div class="row" id="gallery" data-toggle="modal" data-target="#kittenModal">
	<% if @kitten.images.attached? %>
		<% @kitten.images.each_with_index do |img, index| %>
			<div class="col-6 col-sm-3 col-lg-1">
				<img
					src="<%= url_for(img) %>"
					class="w-100"
					data-target="#kittenCarousel"
					data-slide-to="<%= index %>"/>
			</div>
		<% end %>
	<% else %>
		<div class="col-12 col-sm-6 col-lg-3">
			<%= image_tag placekitten, class: "w-100" %>
		</div>
	<% end %>
</div>

<!-- Modal markup: https://getbootstrap.com/docs/4.4/components/modal/ -->
<div class="modal fade" id="kittenModal" tabindex="-1" role="dialog" aria-hidden="true">
	<div class="modal-dialog" role="document">
		<div class="modal-content">
			<div class="modal-header">
				<button type="button" class="close" data-dismiss="modal" aria-label="Close">
					<span aria-hidden="true">×</span>
				</button>
			</div>
			<div class="modal-body">
				
				<!-- Carousel markup: https://getbootstrap.com/docs/4.4/components/carousel/ -->
				<div id="kittenCarousel" class="carousel slide" data-ride="carousel">
					<div class="carousel-inner">
						<% if @kitten.images.attached? %>
							<% @kitten.images.each_with_index do |img, index| %>
							<% if index == 0 %>
									<div class="carousel-item active">
										<img class="d-block w-100" src="<%= url_for(img) %>">
									</div>
									<% else %>
									<div class="carousel-item">
										<img class="d-block w-100" src="<%= url_for(img) %>">
									</div>
							<% end %>
							<% end %>
							<% else %>
							<div class="carousel-item active">
								<img class="d-block w-100" src="<%= placekitten %>">
							</div>
							<% end %>
					</div>
					<a class="carousel-control-prev" href="#kittenCarousel" role="button" data-slide="prev">
						<span class="carousel-control-prev-icon" aria-hidden="true"></span>
						<span class="sr-only">Previous</span>
					</a>
					<a class="carousel-control-next" href="#kittenCarousel" role="button" data-slide="next">
						<span class="carousel-control-next-icon" aria-hidden="true"></span>
						<span class="sr-only">Next</span>
					</a>
				</div>
			</div>
			
			<div class="modal-footer">
				<button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
			</div>
		</div>
	</div>
</div>